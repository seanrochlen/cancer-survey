import React from 'react';
import { shallow } from 'enzyme';
import CancerEntries from '../components/cancerEntries';

let wrapper = shallow(<CancerEntries />);

describe('<CancerEntries>', () => {
  describe('Rendering', () => {
  });
});
